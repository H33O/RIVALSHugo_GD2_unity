using System;
using UnityEngine;

public class BonusBehivior : MonoBehaviour
{
    private void Start()
    {
        Destroy(gameObject,5f);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
